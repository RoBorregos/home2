# numpy is pinned in constraints.txt (must be applied via `pip install -c constraints.txt -r ...`)
# NOTE: Do NOT add opencv-python here.
# The L4T base image ships OpenCV built with CUDA/TensorRT.
# Pip opencv-python replaces it with a CPU-only build.
