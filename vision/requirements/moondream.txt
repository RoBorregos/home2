# numpy is pinned in constraints.txt (must be applied via `pip install -c constraints.txt -r ...`)
# NOTE: Do NOT add opencv-python here, see opencv.txt.
grpcio==1.71.0
protobuf==5.29.1
ultralytics==8.4.19
transformers
Pillow
pyvips
einops
accelerate