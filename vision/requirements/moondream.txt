# numpy is pinned in constraints.txt (must be applied via `pip install -c constraints.txt -r ...`)
opencv-python
grpcio==1.71.0
protobuf==5.29.1
ultralytics==8.4.19
transformers
Pillow
pyvips
einops
accelerate