ARG BASE_IMAGE=dustynv/l4t-pytorch:r36.4.0
FROM ${BASE_IMAGE}

RUN mkdir -p /workspace

# Python Requirements
# On l4t/Jetson, PyTorch comes pre-built from JetPack — skip pip torch to avoid overwriting it.
COPY manipulation/requirements.txt /workspace/requirements/requirements.txt
RUN grep -vE "^torch(vision|audio)?$" /workspace/requirements/requirements.txt \
    > /tmp/requirements_l4t.txt && pip3 install -r /tmp/requirements_l4t.txt
RUN rm -r /workspace/requirements/

RUN chown -R ros:ros /workspace

RUN apt update && apt install -y ros-humble-zed-msgs ros-humble-moveit*

# Install manipulation dependencies
WORKDIR /installs
RUN git clone https://github.com/RoBorregos/home2 --recursive home2_install
WORKDIR /installs/home2_install
RUN rosdep init && rosdep update && rosdep install --from-paths /installs/home2_install/ --ignore-src -r --skip-keys "gpd opencv message_package eigen3 pcl-ros gazebo_ros gazebo_plugins libpcl-dev gazebo_ros2_control" -y
RUN rosdep fix-permissions
WORKDIR /
RUN rm -r installs/

#Copying GPD script and variables
COPY docker/manipulation/setup_gpd.sh /home/ros/setup_gpd.sh
COPY docker/manipulation/setup_contact_graspnet.sh /home/ros/setup_contact_graspnet.sh
RUN chmod +x /home/ros/setup_gpd.sh /home/ros/setup_contact_graspnet.sh
RUN echo ". /home/ros/setup_gpd.sh" >> /home/ros/.bashrc
RUN echo ". /home/ros/setup_contact_graspnet.sh" >> /home/ros/.bashrc
RUN echo "export GPD_INSTALL_DIR=/workspace/install/gpd" >> /home/ros/.bashrc
RUN echo "source /workspace/frida_interfaces_cache/install/setup.bash" >> /home/ros/.bashrc

# CycloneDDS
RUN sudo apt-get update -qq && sudo apt-get install -y -qq ros-humble-rmw-cyclonedds-cpp \
    && sudo rm -rf /var/lib/apt/lists/*
COPY docker/cyclonedds_setup.sh /usr/local/bin/cyclonedds_setup.sh
RUN sudo chmod +x /usr/local/bin/cyclonedds_setup.sh && sudo /usr/local/bin/cyclonedds_setup.sh
ENV RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
ENV CYCLONEDDS_URI=file:///etc/cyclonedds.xml
RUN echo "source /usr/local/bin/cyclonedds_setup.sh" >> /home/ros/.bashrc

WORKDIR /workspace